# Repository Instructions

Before modifying this repository, read and follow the root-level `INSTALLATION_GUIDE.md`.

Its protected-main workflow is mandatory: do not write directly to `main`; use a focused branch, pull request, required tests/CI, review, and only then merge.

If the user explicitly requests review-only work or no repository mutation, do not modify the repository until later authorization.
